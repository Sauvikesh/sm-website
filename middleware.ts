export { default } from 'next-auth/middleware';

export const config = {
  matcher: [
    // '/paramount/errorMessaging',
    // '/paramount/EPG',
    // '/hackdavis',
    // '/sage',
    // '/about',
    // '/',
  ],
};
